動作確認テスト.md — テストのため対応不要（実装依頼ではなく、検知パイプラインの確認用ファイル。依頼本文の指示どおり実装・コミット・pushはしない）

ダークモード対応.md — 【2026-08-30 本人の判断: 案A】リキッドグラスへの作り直しが全画面で終わるまで着手しない。
  ※ A/B/C の選択はもう済んでいる。同じ提案を繰り返さないこと。

  着手できる条件（これを満たすまでは保留のまま、触らない）:
    src/styles/ の作り直しが HOME 以外の7画面でも完了していること。
    2026-08-29 時点で完了しているのは HOME のみ。

  なぜ待つのか:
    いま色トークンになっているのは :root の約20個だけで、実際の色はほとんどが直書き。
    CSSに色リテラルが約563か所（src/index.css + src/styles/*.css で計5,549行）、
    TSX側にも text-slate-500 / bg-white/40 / border-white/45 のような固定色クラスが約394か所（54ファイル）。
    tailwind.config.js に darkMode 設定はなく、dark: バリアントも prefers-color-scheme も未使用、
    index.css は color-scheme: light を明示している。
    これから書き直す7画面を先にトークン化すると、作り直しでもう一度書き直すことになり二度手間になる。

  条件を満たしてから着手するときの進め方:
    1. 色をトークン（CSS変数）へ寄せる。2. tailwind.config.js に darkMode を設定する。
    3. 切り替えは設定画面に置く（システム設定追従＋手動切り替え）。
    4. 全画面が一度に変わる変更なので、本番へ入れる前に見た目を本人が確認する。

PC幅でのレイアウト崩れ修正.md — 【2026-08-30】症状のうち再現できた分は直したが、
  「幅の決め方が壊れている」ぶんは再現できていないので、いったん本人の確認待ち。

  調べた結果（コードを読んだかぎり、幅の計算そのものは壊れていない）:
    1024px以上で .app-shell は display:flex、サイドバーが flex:0 0 13rem（1280px以上は
    14.5rem）、その隣の .app-workspace が flex:1。中身のページ側も
    .planning-page 1620px / .finance-page 1660px / .notes-page 1640px /
    trips・gmail 1480px が margin-inline:auto で、いずれも画面幅に追従する。
    ホームの .hub-grid も1024px以上で12カラムに開く。
    768pxではサイドバーが出ない（出るのは1024px以上）ので、中身は1カラムのまま
    横幅いっぱいに伸びる指定になっている。

  実際に直した分（別依頼「空状態の余白調整」として実装・本番反映済み）:
    お金管理サマリーの給与未登録カードが max-w-4xl のまま左寄せで、1440pxだと
    左に寄ったカード1枚＋右と下が背景だけ、という見え方になっていた。これは
    mx-auto で中央にし、さらに画面の下まで伸びるようにした。
    「中身が左上に張り付いて残りが背景だけ」は、この空状態を見たときの症状と
    ほぼ一致するので、同じものを指している可能性が高い。

  返事がほしいこと（これが分かるまでは触らない）:
    上記を本番に入れたあとでも、まだ幅が余って見える画面があるか。
    あるなら、その画面名と幅（例: 1440pxのメモ・リスト一覧）と、できれば
    スクリーンショットを1枚。データが入っている状態か空の状態かも書いてほしい。

  なぜ勝手に直さないのか:
    幅の土台（.app-shell / .app-workspace / 各ページの max-width）は全画面に一度に
    効く場所で、いま壊れている根拠がコード上に見つからない。見えていない症状に
    合わせて当てずっぽうで触ると、いま正しく出ている画面まで巻き添えになる。
    このセッションはヘッドレス実行でブラウザを起動できず、320/390/1440px の
    スクリーンショット確認（このプロジェクトのUI作業の確認基準）が取れない。
