node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.claude/settings.local.json
artifacts/
img/
.netlify

# Cowork連携の生成物（state.tsv と waiting.txt は共有したいのでコミットする）

# Cowork側のサンドボックスが .git/index.lock を削除できず退避した残骸（手動で消してOK）
_to_delete/

# Cowork の状態は ../.cowork/apps/<アプリ名>/ に集約した（2026-08-31）。
# ここに再びできても追跡しない。
.cowork/
