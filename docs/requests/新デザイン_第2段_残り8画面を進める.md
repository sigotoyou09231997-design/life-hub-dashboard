# 新デザイン第2段：残り8画面を進める

## 何を変えたいか

`デザインを暖色・写真ベースの新デザインに全面刷新.md`の第1段（土台＋ホーム）はOK。
下ナビ／ヘッダーの透けバグも直った。続けて第2段、残り8画面のカードの組み方・写真の使い方を
参考デザインのトーンに合わせて進めてほしい。ブランチは引き続き `design/warm-photo-refresh`。
今回も本番（main）へ入れる前に、区切りのいいところで止めて確認を挟んでほしい。

対象（サイドバーの並び順）：
予定・タスク（今日／カレンダー／一覧／就活）→ お金管理 → メモ・リスト（メモ／日記／
チェックリスト／買い物リスト）→ Gmail → 旅行計画（一覧・詳細）→ ふりかえり（週ごと／月ごと）→
設定 → アカウント。

## ついでに直してほしいもの（下ナビ調査のときに見つかったもの）

前回、下ナビの透けバグを調べたときに `.cowork/waiting.txt` に書いてくれていた2件も、
このタイミングで一緒に直してほしい。

1. `src/hooks/usePageMotion.ts:96` の `.hub-home` 判定が、クラス名が `.warm-home` に
   変わったことで当たらなくなっている。今のクラス名に合わせて直してほしい。
2. `src/hooks/useHubMotion.ts` に、`usePageMotion.ts` にはある「交差が発火しなかった時の
   保険（2秒ごとのfailsafe）」が無い。ホームは要素の6/9が最初 `opacity:0` から始まる作りなので、
   発火しないとホームが真っ白になるリスクがある。`usePageMotion.ts` 側と同じ考え方で保険を足してほしい。

## どの画面／どのデータか

上記8画面のUIコンポーネント・CSSのみ。データ・挙動は変えない（第1段の依頼と同じ方針）。

## 既存データをどう扱うか

触らない。表示のみの変更。

## 進め方について

第1段のときと同じで、規模が大きいので複数回のセッション・コミットに分けて構わない。
全部終わった区切り、あるいはキリのいいところで一度確認を挟んでほしい。
