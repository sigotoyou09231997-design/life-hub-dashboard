# SQL 024 を本番で実行しました。同期登録の2段階目に進んでください

## 何を変えたいか

`supabase/sql/024_category_budgets.sql` を Supabase の SQL Editor で実行し、成功しました（エラーなし）。

021/022と同じ案内どおり、2段階目として `src/lib/syncRuntime.ts` に以下を足してください。

```
module.registerSyncedTable(db.categoryBudgets, "category_budgets");
```

登録後、型チェック・テスト・ビルドを通してコミットしてください。これでカテゴリ別予算が
端末間で揃い、アプリを開いていない日でもWeb Pushで使いすぎ予測を知らせられるようになります。

## どの画面／どのデータか

`src/lib/syncRuntime.ts` の同期登録のみです。画面の見た目は変わりません。

## 既存データをどう扱うか

追加登録のみで、既存の同期テーブル・データには影響ありません。
