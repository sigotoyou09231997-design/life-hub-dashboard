# 支出入力で店名からカテゴリを自動推測してほしい

## 何を変えたいか
支出の手入力(Transaction)で、店名(store)を入れるたびに毎回カテゴリを手で選んでいる。
店名からカテゴリの候補を自動で出して、選ぶ手間を減らしたい。

## どの画面／どのデータか
お金管理 → 支出の手入力フォーム(ExpenseForm.tsx)。

## 既存データをどう扱うか
過去の支出データ（同じ店名で過去に選んだカテゴリ）を学習元に使ってよい。
該当する過去データが無い店名は、候補を出さない/未選択のままでよい。
