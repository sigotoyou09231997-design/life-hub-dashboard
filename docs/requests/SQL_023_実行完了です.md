# SQL 023 を本番で実行しました

## 何を変えたいか

`supabase/sql/023_trip_shares.sql` を Supabase の SQL Editor で実行し、成功しました（エラーなし）。
この機能は案内どおり2段階目の作業は不要とのことなので、これで完了のはずです。
旅行のしおり共有が実際に本番で使えるか、念のため一度確認してください（共有をON→リンクを開く→
日記や設定していない項目が出ていないか→共有をOFFにしてリンクが無効になるか、の一連で構いません）。

## どの画面／どのデータか

旅行詳細の共有機能。

## 既存データをどう扱うか

変更なし。
