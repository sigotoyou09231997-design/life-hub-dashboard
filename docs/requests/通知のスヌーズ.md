# 予定・タスクの通知を「あとで」で再通知したい

## 何を変えたいか
予定・タスクの通知(notifyMinutesBefore)が来たとき、今はそれきりで再通知する手段が
ない。「あとで」を選んで、一定時間後にもう一度通知してほしい。

## どの画面／どのデータか
プッシュ通知まわり(public/push-sw.js, src/lib/pushNotifications.ts)。

## 既存データをどう扱うか
影響なし。
