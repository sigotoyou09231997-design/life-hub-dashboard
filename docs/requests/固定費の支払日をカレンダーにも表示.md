# 固定費の支払日を予定のカレンダーにも表示したい

## 何を変えたいか
固定費(FixedCost)の支払日(dueDay)は、お金管理の中でしか見えない。
予定・タスクのカレンダー(MonthView)にも、支払日として表示してほしい。

## どの画面／どのデータか
予定・タスク → カレンダー(MonthView.tsx)。

## 既存データをどう扱うか
影響なし。表示を追加するだけで、固定費自体のデータや予定(CalendarEvent)としての
保存は増やさない（カレンダー予定として複製はしない）。
