# 旅行のしおり共有、実装を進めてください

## 何を変えたいか

`判断待ちへの回答_旅行のしおり共有_為替換算SQL_年間集計SQL.md` に書いた3項目（SQL作成、
旅行詳細への共有トグルUI、閲覧専用ページ）について、本人から実装の許可が出ました。
条件はそのファイルに書いたとおり決まっているので、追加の判断は不要です。そのまま着手してください。

## どの画面／どのデータか

`判断待ちへの回答_旅行のしおり共有_為替換算SQL_年間集計SQL.md` と同じです。

## 既存データをどう扱うか

同上（変更なし）。
