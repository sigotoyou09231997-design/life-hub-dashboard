# ダークモードに対応したい

## 何を変えたいか
要件書（LIFE_HUB_CLAUDE_CODE.md）では「初期版には入れないが、将来追加できる構造にする」
とされていた。色はすでにCSS変数（デザイントークン）で管理されているはずなので、
実際にダークモードを追加してほしい。

## どの画面／どのデータか
- アプリ全体（既存のCSS変数・トークンを使っている箇所すべて）
- 切り替えは設定画面に置く（端末のシステム設定に追従する／手動切り替え、どちらでも実装しやすい方でよい）

## 既存データをどう扱うか
データの扱いは変わらない。見た目のみの変更。
