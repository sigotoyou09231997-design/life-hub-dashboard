# LIFE HUB — 作業ルール

## 本番反映（2026-08-23、本人の指示）

依頼された作業が終わったら、**その都度の確認を取らずに `main` へマージ・プッシュしてよい**。
`main` へのプッシュはそのまま本番公開（Netlifyの自動ビルド）になる。

条件と守ること:

- `npx tsc --noEmit` と `npm test` が**すべて通った場合のみ** `main` に入れる。
  1つでも落ちたら `main` には入れず、何が落ちたかを報告して止まる。
- テストを飛ばす・無効にする・削除して通すことで条件を満たしたことにはしない。
- 強制プッシュ（`--force`）と履歴の書き換えはしない。
- 作業は指定された作業ブランチで行い、そこへプッシュしてから `main` にマージする。
- 何を本番に入れたかは毎回報告する。
- 依頼された範囲を超える変更は、勝手に足して本番に入れない。

反映後、スマホ側は更新バナーを押すか、アプリを閉じて開き直すと切り替わる
（PWAが `registerType: "prompt"` のため、勝手にはリロードされない）。

## 動作確認のしかた

- 型チェック: `npx tsc --noEmit`
- テスト: `npm test`（Vitest）
- いま公開されているビルドの元コミット: 公開サイトの `/version.json`
  （`vite.config.ts` がビルド時に Netlify の `COMMIT_REF` を書き出している）
