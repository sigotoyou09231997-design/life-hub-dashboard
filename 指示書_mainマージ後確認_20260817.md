# 指示書: mainマージ後の本番確認とブランチ整理

## 現状（`ad5017b`時点）

- `main`は`ed9007c..ad5017b`でfast-forwardマージ済み、push済み
- 両ホストが本番として生きている状態:
  - **Vercel**: `api/tokenExchange.ts` / `api/generateDraft.ts`（新規） + `checkGmailAndNotify`以外
  - **Netlify**: `netlify/functions/checkGmailAndNotify.ts`（10分ごとのGmail監視）+ `public/_redirects`経由で`/api/*`を`netlify/functions/tokenExchange.ts`・`generateDraft.ts`にプロキシ（フロント側の`src/lib/gmail.ts`が`/api/*`しか呼ばなくなったための互換維持）
- ⚠️ **重要**: これにより`netlify/functions/tokenExchange.ts`と`generateDraft.ts`は「削除してよい重複コード」ではなく「Netlify本番サイトの動作に必須のコード」になりました。過去に送った削除提案の指示書は無効です。今後この2ファイルを消してよいのは、Netlifyサイト自体を廃止する時だけです。

## 前提として先にやっておくこと（あなた本人の作業・ダッシュボード）

- [ ] Vercel: 該当プロジェクトの**Deployments**タブで、`ad5017b`（または最新main）を元にした本番デプロイが成功しているか確認する
- [ ] Netlify: 該当サイトの**Deploys**タブで、同じく最新mainを元にしたデプロイが成功しているか確認する（`public/_redirects`が新規追加されたコミットなので、ビルド後の`dist/_redirects`に反映されているか、Netlifyの「Redirects」設定画面で`/api/* → /.netlify/functions/:splat`が認識されているかも合わせて確認するとより確実）

## 動作確認（両方のURLで、それぞれ実施）

以下を**VercelドメインとNetlifyドメインの両方**で確認してください（どちらも本番として生きているため）。

1. トップページが開ける
2. アカウントのログイン（Supabase Auth経由のGoogleログイン）ができる
3. Gmail連携（OAuth同意画面 → `/gmail/callback`）が通り、受信トレイが表示される
4. 受信メールに対してAI下書き生成（`/api/generateDraft`）が動く

いずれかのURLで失敗した場合は、そのURL・エラー内容・ブラウザの開発者ツールのコンソール/ネットワークタブのエラーをそのまま教えてください。原因を切り分けます。

## Claude Codeへの指示（動作確認がOKだった後、任意）

リモートのfeatureブランチ`claude/folder-contents-review-6t7v4t`は、fast-forwardマージによって`main`に完全に取り込まれており、これだけに存在する未マージのコミットはありません。削除して問題ありません。削除する場合は以下をClaude Codeに渡してください。

```
リモートブランチ claude/folder-contents-review-6t7v4t を削除してください
（git push origin --delete claude/folder-contents-review-6t7v4t）。
main は fast-forward でこのブランチを完全に取り込み済みのため、
このブランチだけに残るコミットが無いことを確認したうえで削除してください。
```

保持しておきたい場合は何もしなくて構いません。
