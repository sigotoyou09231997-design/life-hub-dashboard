# LIFE HUB は全体がログインの内側にあるので、そもそも拾えるページはほとんど無い。
# ただし旅行のしおりの共有リンク(/share/trip/<合鍵>)だけはログイン不要で開けるので、
# 「リンクを知っている人だけ」を保つために検索エンジンには拾わせない
# (2026-09-04の指示。ページ側にも noindex を出している — src/pages/SharedTripPage.tsx)。
User-agent: *
Disallow: /share/
